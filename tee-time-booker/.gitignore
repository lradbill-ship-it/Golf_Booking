# Secrets — never commit real credentials
.env
.dashboard.env
config.yaml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# Playwright / runtime artifacts
screenshots/
*.log
auth_state.json

# Dashboard runtime state (kill-switch flag, etc.)
state/
